{
    "_comment": "JSON VALIDATOR http://jsonlint.com/", 
    "metadata": {
        "status": 1, 
        "pipeline": [
            "flow"
        ], 
        "endpoint": "/osc", 
        "plugin_version": "1.0", 
        "plugin_name": "SDN", 
        "plugin_description": "SDN Plugin controller", 
        "type": "scoring"
    }, 
    "action_schema": {
        "formData": {
            "select": ""
        }, 
        "schema": {
            "required": [
                "action"
            ], 
            "type": "object", 
            "properties": {
                "action": {
                    "enumNames": [
                        "BlockHost" 
                    ], 
                    "enum": [
                        "BlockHost" 
                    ], 
                    "type": "string", 
                    "title": "Select action"
                }
            }
        }, 
        "method": "query ($action: String!, $ip: SpotIpType!){osc{executeAction(action: $action, ip: $ip) {action,ip,status,msg}}}", 
        "uiSchema": {
            "action": {
                "ui:placeholder": "Choose an option"
            }
        }
    } 
}